---
- hosts: virtuaalikone
  remote_user: adminuser
  sudo: yes
  tasks:
    - name: what is a jail
      jail:
        root_folder: /var/jail
        commands:
          - /bin/bash
          - /bin/ls
